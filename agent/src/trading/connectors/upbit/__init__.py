"""Upbit trading connector.

Read-only and locally-simulated paper account access via Upbit's public REST
API (JWT-signed, no third-party SDK required). A ``broker_sdk`` transport for
the Korean won crypto markets (``KRW-*``).

Upbit provides free API access with an account-scoped access/secret key pair.
It exposes no sandbox and no runtime paper/live discriminator.
"""
